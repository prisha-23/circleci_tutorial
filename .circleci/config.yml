
version: 2.1

jobs:
  build:
    docker:
      - image: cimg/node:17.2-browsers
    steps:
      - checkout
      - setup_remote_docker:
            docker_layer_caching: true
      - run: docker build .
          #name: "Say hello world"
          #command: "echo Hello, World!"
     
workflows:
  build-deploy:
    jobs:
      - build
